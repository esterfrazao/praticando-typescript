let a: number = 20

const b: number = 1.50

let fullName: string = 'Ester Táfnis'

const isActive: boolean = true

console.log(a,b, fullName, isActive)

let firstNumber: number;
let message: string;
let currenteDate: Date;
let userMessages: string[];
interface Person {
    name: string;
    age: number;
    isMaried: boolean;
}
let personList: Person[]